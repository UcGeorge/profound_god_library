import 'package:flutter/material.dart';

const Icon continueReadingIcon = Icon(
  Icons.play_arrow_rounded,
  color: Colors.green,
  size: 18,
);

const Icon startReadingIcon = Icon(
  Icons.play_arrow_rounded,
  color: Colors.black,
  size: 18,
);

const Icon addToLibraryIcon = Icon(
  Icons.add,
  color: Colors.black,
  size: 18,
);

final Icon deleteIcon = Icon(
  Icons.delete,
  color: Colors.black.withOpacity(0.8),
  size: 16,
);
